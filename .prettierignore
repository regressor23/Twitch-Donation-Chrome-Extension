node_modules
dist
.next
target
pnpm-lock.yaml

# Hand-written docs: CLAUDE.md, the plan and the append-only journal
# must never be reflowed by a formatter.
*.md

# Generated by Next.js, regenerated on every build.
web/next-env.d.ts

# Copied verbatim from `anchor build` output by program/scripts/sync-idl.mjs.
# Reformatting them would put them permanently out of step with the build and
# make `pnpm --filter @tipvault/program check:idl` fail on every run.
program/idl/
program/types/
